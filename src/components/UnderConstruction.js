import React from 'react'
import Loading from './Loading'

export default function UnderConstruction() {
  return (
    <div>
      <Loading hasIcon={false}>Sorry, this page is still under construction!</Loading>
    </div>
  )
}
